const Search = ({title}) => (
    <div className="search">
        <div className="title">{title}</div>
        <div>
            Search:&nbsp;<input type="text"/>
        </div>
    </div>
)

export default Search